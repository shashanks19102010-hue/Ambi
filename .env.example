# Server-side web search provider.
TAVILY_API_KEY=
AMBI_WEB_SEARCH_ENABLED=0

# Local development cloud inference.
# Vercel deployments can use AI Gateway/OIDC without putting a provider key in the browser.
AI_GATEWAY_API_KEY=
AMBI_CLOUD_MODEL=openai/gpt-5.4-mini
