# Optional web search provider. Ambi works locally without any key.
# Set a server-side provider only if you want remote web search.
TAVILY_API_KEY=

# Optional: set to 1 to make /api/search available.
AMBI_WEB_SEARCH_ENABLED=0